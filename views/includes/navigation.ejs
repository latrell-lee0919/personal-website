<nav class="navbar sticky-top navbar-expand-sm navbar-light bg-light">
    <button class="navbar-toggler" data-toggle="collapse" data-target="#myNav2"> <span
            class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="/" class="<%= path ==='/home' ? 'nav-link active' : 'nav-link' %>">Home</a>
            </li>
            <li class="nav-item">
                <a href="/links" class="<%= path ==='/links' ? 'nav-link active' : 'nav-link' %>">Links</a>
            </li>
            <li class="nav-item">
                <a href="/contact" class="<%= path ==='/contact' ? 'nav-link active' : 'nav-link' %>">Contact</a>
            </li>
            <li class="nav-item">
                <a href="/blogs" class="<%= path ==='/blogs' ? 'nav-link active' : 'nav-link' %>">Blogs</a>
            </li>
            <% if (isAdmin) {%>
                <li class="nav-item">
                    <a href="/admin" class="<%= path ==='/admin' ? 'nav-link active' : 'nav-link' %>">Add Blog</a>
                </li>
                <% } %>
        </ul>
        <ul class="navbar-nav ml-auto">
            <% if (isAdmin) { %>
                <li class="nav-item">
                    <form action="/logout" method="POST">
                        <button type="submit" class="btn btn-danger">Log out</button>
                    </form>
                </li>
                <% } %>
        </ul>
    </div>
</nav>