<nav class="navbar sticky-top navbar-expand-sm navbar-light bg-light">
    <a class="navbar-brand" href="/">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"> 
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="/contact" class="<%= path ==='/contact' ? 'nav-link active' : 'nav-link' %>">Contact</a>
            </li>
            <li class="nav-item">
                <a href="/projects" class="<%= path ==='/projects' ? 'nav-link active' : 'nav-link' %>">Projects</a>
            </li>
            <% if (isAdmin) {%>
                <li class="nav-item">
                    <a href="/admin" class="<%= path ==='/admin' ? 'nav-link active' : 'nav-link' %>">Add Project</a>
                </li>
                <% } %>
        </ul>
        <ul class="navbar-nav ml-auto">
            <% if (isAdmin) { %>
                <li class="nav-item">
                    <form action="/logout" method="POST">
                        <button type="submit" class="btn btn-danger">Log out</button>
                    </form>
                </li>
                <% } %>
        </ul>
    </div>
</nav>